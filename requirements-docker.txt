# Ryzanstein LLM — Docker runtime dependencies
# CPU-only torch build (no CUDA) keeps the image ~1.5 GB instead of ~7 GB.
# Install with: pip install -r requirements-docker.txt
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
safetensors==0.4.3
httpx==0.27.0
anyio==4.4.0
