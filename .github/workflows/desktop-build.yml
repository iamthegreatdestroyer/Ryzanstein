name: Desktop App Build & Release

on:
    push:
        branches: [main, phase3/distributed-serving]
        paths:
            - "desktop/**"
            - ".github/workflows/desktop-build.yml"
    pull_request:
        branches: [main]
    workflow_dispatch:

env:
    GO_VERSION: "1.21"
    NODE_VERSION: "20"
    WAILS_VERSION: "2.5"

jobs:
    build:
        strategy:
            matrix:
                os: [ubuntu-latest, windows-latest, macos-latest]
                include:
                    - os: ubuntu-latest
                      platform: linux
                      artifact_name: Ryzanstein.AppImage
                    - os: windows-latest
                      platform: windows
                      artifact_name: Ryzanstein.exe
                    - os: macos-latest
                      platform: darwin
                      artifact_name: Ryzanstein.dmg

        runs-on: ${{ matrix.os }}
        name: Build on ${{ matrix.os }}

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Setup Go
              uses: actions/setup-go@v4
              with:
                  go-version: ${{ env.GO_VERSION }}

            - name: Setup Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: ${{ env.NODE_VERSION }}

            - name: Install Wails
              run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

            - name: Install system dependencies (Linux)
              if: runner.os == 'Linux'
              run: |
                  sudo apt-get update
                  sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev
                  # Wails v2 hard-codes webkit2gtk-4.0 in pkg-config; alias to 4.1
                  PC_DIR=/usr/lib/x86_64-linux-gnu/pkgconfig
                  for pc in webkit2gtk-4.0 javascriptcoregtk-4.0; do
                    new="${pc/4.0/4.1}"
                    sudo cp "$PC_DIR/$new.pc" "$PC_DIR/$pc.pc" 2>/dev/null || true
                  done

            - name: Install system dependencies (macOS)
              if: runner.os == 'macOS'
              run: |
                  brew install create-dmg

            - name: Install frontend dependencies
              working-directory: desktop/frontend
              run: npm install
              continue-on-error: true

            - name: Build Desktop App
              working-directory: desktop
              run: |
                  VERSION=$(grep -m1 '"version"' wails.json 2>/dev/null | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' || echo "0.1.0")
                  mkdir -p dist
                  wails build -ldflags "-X 'main.version=${VERSION}'"

            - name: Upload artifacts
              uses: actions/upload-artifact@v4
              with:
                  name: ${{ matrix.platform }}-build
                  path: desktop/dist/

            - name: Create Release
              if: startsWith(github.ref, 'refs/tags/v')
              uses: softprops/action-gh-release@v1
              with:
                  files: desktop/dist/*
                  draft: false
                  prerelease: false
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    test:
        runs-on: ubuntu-latest
        needs: build

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Setup Go
              uses: actions/setup-go@v4
              with:
                  go-version: ${{ env.GO_VERSION }}

            - name: Run tests
              working-directory: desktop
              run: |
                  go test ./... -v -race -coverprofile=coverage.out

            - name: Upload coverage
              uses: codecov/codecov-action@v3
              with:
                  files: ./desktop/coverage.out
                  flags: desktop-app

    quality:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Setup Go
              uses: actions/setup-go@v4
              with:
                  go-version: ${{ env.GO_VERSION }}

            - name: Run linter
              uses: golangci/golangci-lint-action@v3
              with:
                  working-directory: desktop
                  version: latest

            - name: Security scan
              run: |
                  go install github.com/securego/gosec/v2/cmd/gosec@latest
                  gosec -exclude=G104,G706 ./desktop/...
